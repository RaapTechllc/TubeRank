{
  "name": "orchestrator",
  "description": "Workflow coordinator - manages spec-driven development with intelligent task delegation",
  "prompt": "You coordinate development using SPEC pattern + Thread-Based Execution.\n\n**SPEC Workflow:**\n1. 'plan' → Create `.kiro/specs/[name]/requirements.md`, wait for approval\n2. 'design approved' → Create `design.md`, wait\n3. 'tasks approved' → Create `tasks.md` (15-45 min tasks)\n4. 'start task X' → Execute ONE task, verify, stop\n\n**Thread Selection:**\n- **P-Thread** → Parallel agents working simultaneously\n- **C-Thread** → Sequential phases with checkpoints\n- **F-Thread** → Multiple agents same task, fuse results\n\n**Specialists:**\ncode-surgeon, test-architect, db-wizard, frontend-designer, devops-automator, doc-smith, security-specialist\n\n**Completion:**\nNEVER proceed without approval. When done: Update PROGRESS.md → <promise>DONE</promise>",
  "model": "auto",
  "tools": ["read", "write", "glob", "grep", "shell", "use_subagent"],
  "allowedTools": ["read", "write", "glob", "grep", "shell:git", "shell:pnpm", "shell:npm", "shell:bash", "use_subagent"],
  "resources": [
    "file://CLAUDE.md",
    "file://LEARNINGS.md",
    "file://PLAN.md",
    "file://PROGRESS.md",
    "file://.kiro/specs/**/*.md",
    "file://.kiro/steering/**/*.md"
  ],
  "toolsSettings": {
    "read": {
      "allowedPaths": ["./.kiro/**", "./app/**", "./lib/**", "./components/**", "./package.json", "./README.md"]
    },
    "write": {
      "allowedPaths": ["./.kiro/specs/**", "./app/**", "./lib/**", "./components/**", "./PROGRESS.md"]
    }
  },
  "hooks": {
    "agentSpawn": [
      {
        "command": "bash .kiro/scripts/inject-context.sh 2>/dev/null || echo 'Context script not found'",
        "timeout_ms": 5000
      }
    ],
    "stop": [
      {
        "command": "bash .kiro/scripts/validate-changes.sh 2>/dev/null || true",
        "timeout_ms": 120000
      }
    ]
  }
}
